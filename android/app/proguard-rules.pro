# Flutter's own classes are kept by the engine's consumer rules; nothing in
# this app uses reflection, so no extra keeps are needed here. The file exists
# so release builds have an explicit, reviewable place for future rules.
